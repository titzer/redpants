echo Cleaning up .o files...
make clean > junk
echo Making ELF image...
make > junk
# echo Mounting floppy...
# mount /mnt/floppy
echo Copying kernel image...
cp -f rpkernel.elf /mnt/floppy
# echo Unmounting floppy...
# umount /mnt/floppy

echo Kernel built and boot disk updated.

rm -f junk
